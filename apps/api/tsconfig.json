{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "lib": ["ES2020"],
    "typeRoots": [
      "${configDir}/node_modules/@types",
      "../../node_modules/@types"
    ]
  }
}
