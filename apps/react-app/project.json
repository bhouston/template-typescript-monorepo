{
  "name": "react-app",
  "root": "apps/react-app",
  "targets": {
    "watch": {
      "options": {
        "commands": ["bunx vite"]
      }
    },
    "build": {
      "options": {
        "commands": ["bunx tsc", "bunx vite build"]
      }
    },
    "lint": {},
    "format": {}
  }
}
