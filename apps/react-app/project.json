{
  "name": "react-app",
  "root": "apps/react-app",
  "targets": {
    "watch": {
      "options": {
        "commands": ["vite"]
      }
    },
    "build": {
      "options": {
        "commands": ["tsc", "vite build"]
      }
    },
    "lint:check": {},
    "lint": {},
    "format": {}
  }
}
